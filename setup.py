from setuptools import setup

setup(
   name='graphae',
   version='0.1',
   description='...',
   author='xxx',
   author_email='xxx',
   packages=['graphae'], install_requires=['torch', 'rdkit', 'numpy']
  )
