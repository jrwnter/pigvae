from setuptools import setup

setup(
   name='pigvae',
   version='0.1',
   description='...',
   author='xxx',
   author_email='xxx',
   packages=['pigvae'], install_requires=['torch', 'rdkit', 'numpy']
  )
